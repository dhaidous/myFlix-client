import React
    from "react";
export const ProfileView = ({ user, movies }) => {
    // Your component code
    return (
        <div>
            <h1>This is the profile page</h1>
        </div>
    );
};
